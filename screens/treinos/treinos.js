import React from "react";
import { View, Text } from "react-native";

export default Treinos = () => {
    return(
        <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
            <Text>Bem vindo a sessão de treinos</Text>
        </View>
    )
}